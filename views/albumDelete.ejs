<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head')%>
  <body>
    <%- include('partials/header')%>
    <main>
   
      <a href="/albums/<%= album.id %>/detail"><img src="/imgs/back.svg"/></a>
      <h1><%= title %></h1>
      <h2>Are you sure you want to delete this album?</h2>
      <h1><%= album.title %></h1>
      <p>Release year: <%= album.year %></p>
      <p>Artist(s):</p>
      <ul>
        <% if (locals.artistList) { %> <% artistList.forEach((artist) => {%>

        <li>
          <a href="/artists/<%= artist.id %>/detail"><%= artist.name %></a>
        </li>
        <% }) %> <% } %>
      </ul>
      
      <p><%= album.notes %></p>
      <form method="POST"><button>Delete</button></form>
    </main>
    <%- include('partials/footer')%>
  </body>
</html>
