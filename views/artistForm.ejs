<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head')%>

  <body>
    <%- include('partials/header')%>

    <main>
      <h1><%= title %></h1>
      <p>TBA: conditional link to list or item</p>
      <a href="/artists">Back to Artists</a>
      <%- include('partials/errors')%>
      <form method="POST">
        <label for="name">Artist/Group name:</label>
        <input type="text" name="name" minlength="1" maxlength="60" value="<%= (locals.artist) ? artist.name : '' %>" />
        <label for="activeyear">First year active:</label>
        <input type="number" name="activeyear" min="1800" max="2100" value="<%= (locals.artist) ? artist.activeyear : '' %>" />
        <label for="notes">Notes:</label>
        <textarea name="notes"  minlength="1" maxlength="1000"><%= (locals.artist) ? artist.notes : '' %></textarea> 
        <button>Submit</button>
      </form>
    </main>
    <%- include('partials/footer')%>

  </body>
</html
